# Arc Tunnel — E2E Test Suite

**Date:** 2026-05-30  
**Method:** Self-bootstrapping — AI calls arc-tunnel MCP tools on real browser tabs and validates responses.

## Why No Test Framework

Jest cannot test extension-side code (chrome.debugger, chrome.tabs, chrome.storage) — these browser APIs don't exist in Node. The MCP server side (command-queue, types, tools, websocket-server) already has 14 unit tests.

The most realistic test is AI directly invoking MCP tools against a live browser — the same path real users take.

## Test Plan

### Prerequisites
- Arc Tunnel extension loaded and connected in browser
- At least one tab open (any URL)
- AI Agent tool (Claude Code, Kimi Code CLI, Hermes, OpenClaw, etc.) with arc-tunnel MCP server connected

### Important
**These test cases must be executed in order.** Later tests depend on state from earlier ones (recordingId, sessionId, tabId).

### Test Cases (15 tools)

| # | Tool | Action | Expected |
|---|------|--------|----------|
| 1 | `list_tabs` | Call without params | Returns `{ tabs: [...] }` with ≥1 tabs |
| 2 | `navigate` | `{ tabId, action: "goto", url: "https://example.com" }` | `{ status: "navigated" }` |
| 3 | `snapshot` | `{ tabId }` on example.com | Returns tree with at least one link element (e.g. "More information...") |
| 4 | `execute_script` | `{ tabId, script: "document.title" }` | Returns `{ result: "Example Domain" }` |
| 5 | `interact` | `{ tabId, action: "hover", target: "e1" }` | `{ status: "hover" }` (non-destructive, stays on page) |
| 6 | `screenshot` | `{ tabId }` | Returns base64 PNG data (iVBOR header) |
| 7 | `start_recording` | `{ tabId }` | Returns `{ recordingId: "..." }` |
| 8 | `stop_recording` | Call without params | Returns `{ recording: { actions: [...] } }` |
| 9 | `replay_recording` | `{ recordingId, tabId }` — recordingId from tests 7–8 above | `{ status: "replayed" }` |
| 10 | `create_tab` | `{ url: "https://example.com" }` | Returns `{ tabId: number }` |
| 11 | `get_console_logs` | `{ tabId }` — on the new tab from test 10 | Returns `{ logs: [...] }` (empty array is valid) |
| 12 | `manage_storage` | `{ tabId, type: "cookie", action: "list" }` — on the new tab | Returns `{ cookies: [...] }` |
| 13 | `close_tab` | `{ tabId }` — closes the tab from test 10 | `{ status: "closed" }` |
| 14 | `save_session` | `{ name: "e2e-test" }` | Returns `{ sessionId: "..." }` |
| 15 | `restore_session` | `{ sessionId }` — sessionId from test 14 above | `{ status: "restored" }` |

## Manual Execution

Paste this prompt in a fresh Claude Code session:

> Run the arc-tunnel E2E test suite. Test all 15 tools in order against a real browser tab. For each tool, record pass/fail and any error messages. Report the results as a table.

## Automated CI (Future)

When arc-tunnel supports headless browser control without user input:

```bash
# Pseudocode — requires headless browser support (not yet available)
claude --print "Run the arc-tunnel E2E test suite..." 
```

## History

| Date | Result |
|------|--------|
| 2026-05-30 | 15/15 passed — all tools functional |
